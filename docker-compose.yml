version: '3'
services:
  minecraft-server:
    build:
      context: .
    env_file:
      - .env
    ports:
      - "25565:25565"
    volumes:
      - data:/server
      - ./server-files/server.properties:/server/server.properties
      - ./server-files/backups:/server/backups
    networks:
      - net

networks:
  net:

volumes:
  data: